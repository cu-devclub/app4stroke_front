import Typography from "@material-ui/core/Typography";
import React from "react";

const SectionTitle: React.FC = () => {
  return <Typography>Patient Information</Typography>;
};

export default SectionTitle;
